apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-deployment
  namespace: spotlink-dev
spec:
  replicas: 2
  selector:
    matchLabels:
      app: web
  template:
    metadata:
      labels:
        app: web
    spec:
      nodeSelector:
        environment: dev
      containers:
      - name: web
        image: 440744234807.dkr.ecr.us-east-1.amazonaws.com/spotlink/web:develop
        ports:
        - containerPort: 8080
        env:
        - name: PORT
          value: "8080"
---
apiVersion: v1
kind: Service
metadata:
  name: web-service
  namespace: spotlink-dev
spec:
  selector:
    app: web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: web-ingress
  namespace: spotlink-dev
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-prod" # Ajusta según tu issuer
spec:
  rules:
  - host: web-dev.spotlink.app
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: web-service
            port:
              number: 80
  tls:
  - hosts:
    - web-dev.spotlink.app
    secretName: web-dev-tls
